#!/bin/bash
#PBS -N train_final
#PBS -l select=1:ncpus=4:mem=8gb
#PBS -l walltime=24:00:00
#PBS -j oe

cd $PBS_O_WORKDIR

LOGDIR=logs
mkdir -p ${LOGDIR}

python clean_codes_v2/run_train_pipeline.py \
    --config-file demo_config.json \
    --train 1 \
    > ${LOGDIR}/final_train.log 2>&1